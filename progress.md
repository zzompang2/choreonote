# 진행 상황

## 현재 상태: 에디터 UX 개선 진행 중

### 최근 완료 (2026-04-16)
- 댄서 편집 [경유점 초기화] 버튼 추가
  - 동선 구간 선택 + 댄서 1명 이상 선택 시 활성
  - 선택된 댄서들의 경유점을 중간점(t=0.5)으로 일괄 초기화
  - [선택 댄서로 대형 만들기]와 같은 actions 영역에 배치
  - 두 버튼 모두 1명 이상 선택 시 표시, 컨텍스트에 따라 disabled
- Ctrl+S / Cmd+S 노트 저장 단축키 추가 (단축키 모달에도 반영)
- btn--ghost disabled 스타일 추가 (opacity 0.35, cursor not-allowed)
- 마켓 업로드 대형 범위 선택 UI (체크박스 → 클릭-클릭 범위 선택, 최대 5개)
- share.js 테스트 mock 데이터 제거

### 이전 완료
- UI 용어 전면 통일 ("대열"→"대형", "동선 구간", "동선")
- 대형 마켓 1단계 구현 (Google OAuth, 카드 그리드, 업로드/가져오기)

### 결정사항 (유효)
- **대형 마켓 방향**: 유저 업로드 마켓(B안), 하이브리드 진입(C안), 3단계 로드맵
- **인증 정책**: 공유 링크는 익명 유지, 마켓 가져오기/업로드만 로그인 필요
- **업로드 단위**: 노트 전체가 아닌 특정 대형 선택 업로드 (범위 선택, 최대 5개)
- **용어 규칙**: 대형(Formation), 동선 구간(대형 사이 전환 영역), 동선(댄서 이동 경로)
- **인스펙터 버튼 규칙**: 1명 이상 선택 시 표시, 컨텍스트에 맞지 않으면 숨기지 않고 disabled

## 다음 할 일
- [ ] 대형 마켓 추가 개선 (유저 피드백 반영)
- [ ] 대형 마켓 2단계: 사이드바 "마켓에서 가져오기" 버튼 + 내 프리셋 자동 추가
- [ ] 대형 마켓 3단계: 좋아요/인기순 정렬, 검색, 카테고리

### 챗봇 미구현/개선 가능 사항
- 한영 자동 변환 미구현 ("ekstp" → "댄서" 등)
- 양쪽 FAQ 동시 검색 미구현

## 컨텍스트
- Supabase 프로젝트: `gflnxqrvzlydyjmokyep.supabase.co` (서울 리전)
- `shares` 테이블 (공유 링크, 익명)
- `market_presets` 테이블 (마켓, 로그인 필요)
- Google OAuth 활성화 완료 (Google Cloud Console + Supabase Provider)
